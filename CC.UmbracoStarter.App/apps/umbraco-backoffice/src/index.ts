/**
 * Example Umbraco backoffice extension entry.
 * Build the project and copy the produced dist files to Umbraco's App_Plugins folder.
 */

import { buttonExtensionManifest } from 'tiptap-extensions';


export const manifests: Array<UmbExtensionManifest> = [buttonExtensionManifest];